name: CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go:
    name: Go checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify go mod
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Format check
        run: |
          test -z "$(gofmt -l .)"

      - name: Build golangci-lint (v1.64.5) from source
        run: |
          GOBIN="$RUNNER_TEMP/bin" go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.64.5
          "$RUNNER_TEMP/bin/golangci-lint" version

      - name: Lint
        run: |
          "$RUNNER_TEMP/bin/golangci-lint" run --timeout=5m

      - name: Test
        run: go test ./...

      - name: Build
        run: go build ./...

  container:
    name: Container checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t h3ws2h1ws-proxy:ci .

      - name: Validate docker compose config
        run: docker compose config
